/**
 * utils part of dalieweb.
 * 
 * All functions to help to make your Live easier.
 * 
 * @since 1.0.0.0
 * 
 */
var utils = {

	isNotEmpty : function(value) {
		return value == typeof (userId) != 'undefined' ? value : false;
	},
	
};