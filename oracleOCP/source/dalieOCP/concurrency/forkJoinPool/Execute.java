package dalieOCP.concurrency.forkJoinPool;

class Execute {

	/*
	 * The fork/join framework is best suited for tasks that are processor
	 * intensive and that can be divided into smaller tasks that can execute
	 * independently, in parallel. Tasks that block, work with I/O, or
	 * need synchronization aren’t good candidates to use with the fork/join framework.
	 * */
	
	
	/*
	 * The fork/join framework is named so because it initiates execution
	 * of a task that forks or starts multiple subtasks, and waits for them to
	 * join back (or complete their execution).
	 * */
	
}
