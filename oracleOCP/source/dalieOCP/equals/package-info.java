/**
 * <ul>
 * <li>Whenever it is invoked on the same object more than once during an execution of a Java application,<br/>
 *  the hashCode method must consistently return the same integer.<br/><br/></li>
 * <li>If two objects are equal according to the equals(Object) method,<br/>
 *  then calling the hashCode method on each of the two objects <br/>
 *  must produce the same integer result.</li>
 * </ul>
 * 
 * 
 * @author dalieuwe
 *
 */
package dalieOCP.equals;