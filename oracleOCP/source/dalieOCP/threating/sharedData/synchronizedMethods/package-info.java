/**
 * <h6></h6>
 * 
 * <ul>
 * 	<li>The methods, which modify the state of instance or static variables, should
 * be defined as synchronized methods. This prevents multiple threads from modifying
 * the shared data in a manner that leads to incorrect values.</li>
 * </ul>
 * @author dalie.de
 *
 */
package dalieOCP.threating.sharedData.synchronizedMethods;