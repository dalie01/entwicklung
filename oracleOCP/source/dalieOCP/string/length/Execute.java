package dalieOCP.string.length;

class Execute {

	public static void main(String[] args) {
		System.out.println("Jena".length());//prints 4
		/* The length of a String is one number greater than the position
		 * that stores its last character. The length of String "Jena" is 4, but its last
		 * character, a, is stored at position 3 because the positions start at 0, not 1.
		 */
	}
}
