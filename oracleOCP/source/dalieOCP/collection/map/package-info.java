/**
 * <h6>Map & HashMap</h6>
 * 
 * <ul>
 * 	<li>duplicate argument isn't possible</li>
 * 	<li>not synchronization</li>
 *  <li>the order of items isn’t guaranteed <br/>
 *  	to be the same as its insertion order.
 *  </li>
 * </ul>
 * 
 * <p>The Map interface doesn’t extend the core Collection interface.</p>
 * 
 * <p>The keys of a HashMap aren’t ordered. The HashMap methods 
 * aren't synchronized, so they aren’t safe to be used in a multi-threaded environment.</p>
 * 
 * @author dalieuwe
 *
 */
package dalieOCP.collection.map;